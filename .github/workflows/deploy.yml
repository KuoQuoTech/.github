name: Deploy KuoQuo Docs Website

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout repo + submodules
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      # 2. Setup Python
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: 3.x

      # 3. Install Zensical
      - name: Install Zensical
        run: pip install zensical

      # 4. Build site
      - name: Build site
        run: zensical build --clean

      # 5. Inject submodule into built site (moved here)
      - name: Prepare submodules
        run: |
          echo "Copying KuoQuo-Pivot into site/pivot..."
          mkdir -p site/pivot
          cp -r KuoQuo-Pivot/* site/pivot/ || true
          
          echo "Copying .gitmodules to site root..."
          cp .gitmodules site/ || true

      # 6. Deploy to gh-pages branch
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./site
          publish_branch: gh-pages